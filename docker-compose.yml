version: '3.8'

services:
  ai-eval-pipeline:
    build: .
    ports:
      - "8081:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    logging:
      driver: "json-file"
      options:
        max-size: "10mb"
        max-file: "3"
